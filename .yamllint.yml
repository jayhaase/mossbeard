---
extends: default

rules:
  line-length:
    max: 120
    level: warning
  document-start: disable
  comments: disable
  colons: disable   # _data files use aligned keys (e.g. `name:        Value`)
  truthy:
    check-keys: false
